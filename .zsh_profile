# add custom scripts to path
export PATH="$HOME/.local/bin:$PATH"
export PATH="$N_PREFIX/bin:$PATH"

# run tmux-sessionizer script while outside of vim
bindkey -s ^f "tmux-sessionizer\n"

# run startup script--start limelight
# startup.sh
